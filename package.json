{
  "private": true,
  "bugs": "https://github.com/nickcis/react-data-ssr/issues",
  "homepage": "https://github.com/nickcis/react-data-ssr#readme",
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap && node ./scripts/bootstrap.js",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "doctoc": "find . -not -wholename '*node_modules*' -name '*.md' -exec grep -q '<!-- START doctoc' {} \\; -exec doctoc {} \\;"
  },
  "devDependencies": {
    "doctoc": "^1.3.0",
    "lerna": "^2.4.0",
    "semver": "^5.4.1",
    "shelljs": "^0.7.8"
  }
}
